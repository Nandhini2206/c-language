#include<stdio.h>
void main()
{
    int a;
    printf("Enter a number");
    scanf("%d",&a);
    int sum=0;
    while(a>=1)
    {
        sum=sum+a%10;
        a=a/10;
    }
    printf("sum of digits is %d",sum);
    
}
